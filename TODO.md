# TODO.md

이 파일은 이 레포에서 여러 세션에 걸쳐 프론트엔드 작업 우선순위를 이어가기 위한 작업 기준 문서다.

원칙:

- 한 번에 전부 해결하려고 하되, 구현은 우선순위 순서대로 진행한다.
- 새 세션을 시작하면 작업 전에 이 파일을 먼저 확인한다.
- 항목을 끝내면 상태를 갱신하고, 범위가 바뀌면 메모를 함께 남긴다.

상태값 규칙:

- `TODO`: 아직 시작하지 않음
- `IN PROGRESS`: 현재 작업 중
- `BLOCKED`: 외부 의존성이나 결정이 필요함
- `DONE`: 완료

## Frontend Priority

### 1. 대표 강의 선택 UX 개선

상태: DONE
우선순위: Highest
최근 업데이트: 2026-04-01
진행 메모: 대표 강의 ID 직접 입력 UI를 검색/선택 방식으로 교체했고, 저장 흐름에 연결 완료.

목표:

- 어드민 화면에서 대표 강의를 ID 직접 입력으로 등록하지 않는다.
- 강의 목록을 가져온 뒤, 그 중 하나 이상을 선택해서 대표 강의로 지정할 수 있게 한다.

구현 메모:

- 어드민 홈 관리 화면에서 현재 대표 강의 등록 UI 확인
- 강의 목록 조회 API 재사용 가능 여부 확인
- 검색/선택 UI 추가
- 선택 결과를 기존 홈 대표 강의 저장 흐름에 연결

---

### 2. 강의 수정 기능

상태: DONE
우선순위: High
최근 업데이트: 2026-04-01
진행 메모: 관리자 상세 화면에서 수정 진입점을 추가했고, 강의 업로드 화면을 수정 모드와 공용으로 전환해 기본 정보/썸네일/비디오 목록 수정이 가능하게 했다.

목표:

- 관리자가 기존 강의를 수정할 수 있게 한다.

구현 메모:

- 수정 진입점 추가
- 강의 기본 정보 수정
- 썸네일 변경
- 비디오 추가/삭제/수정
- 필요 시 백엔드 수정 API와 함께 연결

---

### 3. 비디오 플레이어 16:9 고정

상태: DONE
우선순위: High
최근 업데이트: 2026-04-01
진행 메모: 일반 모드 플레이어 박스를 16:9로 고정했고, 내부 일반 비디오는 `contain` 처리했다. 풀스크린은 화면 전체를 사용하도록 유지했다. 워크트리에서 빌드 검증을 통과했다.

목표:

- 세로 영상이 업로드되어도 일반 모드에서는 플레이어 박스가 항상 16:9를 유지한다.
- 풀스크린에서는 화면 전체를 사용한다.

구현 메모:

- `VideoPlayerView.vue`의 플레이어 컨테이너 구조 점검
- 일반 모드 aspect ratio 고정
- 내부 영상은 `contain` 처리 검토

---

### 4. 리스트뷰 아이템 고정 높이

상태: DONE
우선순위: High
최근 업데이트: 2026-04-01
진행 메모: 리스트뷰 카드 높이와 썸네일 래퍼 높이를 고정하고, 이미지 `object-cover`와 콘텐츠 영역 `h-full` 정렬을 적용함.

목표:

- 모바일/리스트 뷰에서 강의 아이템 높이가 썸네일 원본 비율에 따라 흔들리지 않게 한다.

구현 메모:

- `LectureListItem.vue` 루트 높이 고정
- 썸네일 래퍼 `h-full`
- 이미지 `object-cover`
- 콘텐츠 영역 높이 정렬

---

### 5. 강의 카드 레이아웃 간격 안정화 검토

상태: TODO
우선순위: Medium
최근 업데이트: 없음
진행 메모: 없음

목표:

- 브라우저 크기가 바뀔 때 강의 카드 간격과 폭이 계속 달라지는 문제를 줄인다.
- 현재 레이아웃이 "4개면 꽉 찬 화면"을 암묵적으로 가정하는지 검토한다.

이 항목의 성격:

- 즉시 구현보다 조사와 방향 결정이 먼저다.

검토 포인트:

- 현재 `auto-fit`, `minmax`, breakpoint`, grid/flex` 구조 확인
- 카드 최소폭/최대폭 제한 필요 여부
- 고정 column 수 방식이 더 적합한지 검토
- `CoursesView`, `MyCoursesView`, 홈 강의 섹션 공통 전략 정리

---

### 6. 에러 메시지 개선

상태: TODO
우선순위: Medium
최근 업데이트: 없음
진행 메모: 없음

목표:

- API 실패 시 사용자 친화적인 메시지로 정리한다.
- 단순 `alert()` 위주 흐름을 점진적으로 개선한다.

---

### 7. 반응형 버튼/간격 최적화

상태: TODO
우선순위: Medium
최근 업데이트: 없음
진행 메모: 없음

목표:

- 모바일/태블릿에서 버튼 배치와 간격을 정리한다.

메모:

- 위 기능 작업을 진행하면서 함께 다듬는 것이 효율적일 수 있다.

---

### 8. 관리자 강의 관리 기능 고도화

상태: TODO
우선순위: Medium
최근 업데이트: 없음
진행 메모: 없음

목표:

- 관리자 화면에서 강의 목록 조회, 수정, 기타 관리 흐름을 더 완성도 있게 정리한다.

메모:

- `2. 강의 수정 기능`과 묶여 진행될 가능성이 높다.

## Working Rule

다음 작업을 시작할 때는 가능하면 아래 순서를 따른다.

1. 대표 강의 선택 UX 개선
2. 강의 수정 기능
3. 비디오 플레이어 16:9 고정
4. 리스트뷰 아이템 고정 높이
5. 강의 카드 레이아웃 간격 안정화 검토
6. 에러 메시지 개선
7. 반응형 버튼/간격 최적화
8. 관리자 강의 관리 기능 고도화
